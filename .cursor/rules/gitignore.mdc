---
description: Keep Phantasmon .gitignore current when adding tooling, secrets, or local run artifacts.
alwaysApply: true
---

# Gitignore

When adding secrets, local run dirs, Docker volumes, IDE files, or build outputs, update `.gitignore` in the same change.

Must ignore: `.env` (keep `!.env.template`), `application-local.*`, `run/`, `.gradle/`, `build/`, `pgdata/`, crash dumps, `*.hprof`.

Must **not** ignore: `.cursor/skills/`, `.cursor/rules/`, `gradle/wrapper/gradle-wrapper.jar`, `Documentation/`.

Do not commit secrets even if the user stages them.

# Reference repos (read-only)

Use these workspace clones as documentation. Do not modify them.

| Repo | Typical questions |
|------|-------------------|
| `cobblemon` | Species JSON, abilities, moves, client renderers, party UI patterns, events |
| `fabric` | Client lifecycle, networking (we do **not** use Fabric server networking for ghosts) |
| `pokemon-showdown` | Set format, natures, EV/IV conventions for import |
| `pokemon-showdown-client` | How the web client parses sets |
| `ClothConfig` | Optional later config screens |
| `architectury-api` | Not a V1 dependency (Fabric-only) |

Phantasmon V1 language is **Java**, not Kotlin (`fabric-language-kotlin` is reference only).

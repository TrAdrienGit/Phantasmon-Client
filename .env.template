# Copy this file to .env and fill in local values.
# Never commit .env — it is gitignored.
#
# The Fabric client does not connect to PostgreSQL.
# These keys exist so local tooling can stay aligned with the backend.

# -------------------------------------------- #
# BDD Configuration

BDD_USER=
BDD_PASSWORD=
BDD_NAME=db_phantasmon
BDD_HOST=localhost
BDD_PORT=5432
